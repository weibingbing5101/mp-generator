{
  "usingComponents":{
  	{{#each floors}}
  	{{#if @last}}
  	"{{name}}":"../../components/{{name}}/{{name}}"
  	{{else}}
  	"{{name}}":"../../components/{{name}}/{{name}}",
  	{{/if}}
  	{{/each}}
  },
  "navigationStyle": "custom"
}